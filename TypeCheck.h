#pragma once


namespace typeCheck 
{

// Check if Variable(i.e if it has no command in front of it)

// Create a expandable 2D vector of strings, each element saved as a type in said vector with a stored data type 
}